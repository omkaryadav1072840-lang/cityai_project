# =========================================================
# SmartCity AI - Docker Deployment Environment Variables
# Copy to .env or pass via: docker compose --env-file docker.env up -d
# =========================================================

# Database Credentials
DB_NAME=smartcity
DB_USER=root
DB_PASSWORD=production_secure_mysql_password_2026

# JWT Security
JWT_SECRET=super_secret_enterprise_jwt_token_gorakhpur_smartcity_2026
JWT_EXPIRES_IN=7d

# Emergency & Ambulance Secrets
AMBULANCE_UPDATE_TOKEN=smartcity_ambulance_secure_dispatch_token_2026

# AI Engine (Optional - offline NLP engine runs automatically if empty)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash
